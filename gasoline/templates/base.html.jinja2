<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{% block title %}{{ page_title|default(_('Project')) }}{% endblock %} - Gasoline</title>
    {% assets "css" %}<link href="{{ ASSET_URL }}" rel="stylesheet" media="screen">{% endassets %}
    <meta name="description" content="Gasoline, collaborative wiki">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>

    {% block topbar %}
    <nav class="navbar navbar-default" role="navigation">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">{{ _('Toggle navigation') }}</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Gasoline</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li>
            <form action="{{ url_for('document.edit', space=space | default('main')) }}">
            <button class="btn btn-default navbar-btn">{{ _('Create') }}</button>
            </form>
          </li>
      {% if current_user.is_authenticated() %}
          <li>
            <form class="navbar-form navbar-left form-inline" role="search" action="{{url_for('search.search')}}" method="get" name="search">
              <div class="form-group has-feedback dropdown" id="search_input">
                {{ g.search_form.query(class='form-control', style='width: 180px', autocomplete='off', placeholder=g.search_form.query.description)|safe }}
                <i id="search_button" class="fa fa-search fa-fw form-control-feedback"></i>
                <ul id="search_results" class="dropdown-menu"></ul>
              </div><!-- /input-group -->
            </form>
          </li>
      {% endif %}
        </ul>
      {% if current_user.is_authenticated() %}
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user fa-lg"></i> <i class="fa fa-caret-down"></i></a>
          <ul class="dropdown-menu">
            <li{% if request.path.startswith("/user") %} class="active"{% endif %}>
              <a href="{{ url_for('user.profile', name=current_user.name) }}"><i class="fa fa-user"></i> {{ _('My account') }}</a>
            </li>
            <li class="divider"></li>
            <li><a href="{{ url_for('user.logout') }}"><i class="fa fa-sign-out"></i> {{ _('Logout') }}</a></li>
          </ul>
        </li>
      </ul>
      {% endif %}
      </div><!-- /.navbar-collapse -->
    </nav>
    {% endblock %}

  <div class="content">
    {% with messages = get_flashed_messages(with_categories=true) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="alert alert-{{ category }} alert-dismissable">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <i class="fa {% if category == 'success' %}fa-check{% elif category == 'danger' %}fa-warning{% elif category == 'info' %}fa-info-circle{% endif %}"></i> {{ message }}
          </div>
        {% endfor %}
      {% endif %}
    {% endwith %}

    {% block content %}{% endblock %}
  </div>
    {% assets "js" %}<script type="text/javascript" src="{{ ASSET_URL }}"></script>{% endassets %}
    {% assets "js-fr" %}<script type="text/javascript" src="{{ ASSET_URL }}"></script>{% endassets %}
    {% block js %}{% endblock %}
  </body>
</html>